[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "eams-seof"
version = "0.1.0"
description = "SEOF-based seasonal SST prediction in the East Asian marginal seas"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
